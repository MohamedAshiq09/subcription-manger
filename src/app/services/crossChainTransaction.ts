const processCrossChainTransaction = async (chain: string, amount: number, subscriber: string) => {
    if (chain === 'agoric') {
    } else if (chain === 'ethereum') {
    } else if (chain === 'cosmos') {
    }
  };
  